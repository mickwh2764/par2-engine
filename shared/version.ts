// Single source of truth for the web platform version shown in the UI, in
// exported reports and by the public API. This is the PAR(2) Discovery Engine
// web-app version and is distinct from the `par2-circadian` PyPI package
// version.
export const APP_VERSION = "2.6.0";
